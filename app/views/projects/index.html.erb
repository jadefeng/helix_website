<header>
  <div class="container">
    <div class="intro-text">
      <h1>Case Studies</h1>
      </div>
  </div>
</header>

<h1>javascript modals popping in and out will happen here</h1>
<div class="container">
<% @projects.each do |project| %>
      <div class="col-md-6 col-sm-6 portfolio-item">
        <img src="<%= project.image %>" class="img-responsive"> </a>
        <div class="portfolio-caption">
          <h4><%= project.name %></h4>
          <p class="text-muted"><%= project.description %></p>
        </div>
      </div>

<% end %>
</div>

<br>

<%= link_to 'New Project', new_project_path %>
